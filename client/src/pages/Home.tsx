import { useAuth } from "@/_core/hooks/useAuth";
import DashboardLayout from "@/components/DashboardLayout";
import { Button } from "@/components/ui/button";
import { Textarea } from "@/components/ui/textarea";
import { getDiscoverPreview, getQualifiedPosts } from "@/lib/discoverFeed";
import { personalizedGreeting } from "@/lib/discoverAgent";
import { trpc } from "@/lib/trpc";
import { ArrowRight, CheckCircle2, CircleAlert, Compass, ExternalLink, Inbox, Loader2, Radar, RefreshCw, Search, Sparkles } from "lucide-react";
import { FormEvent, useEffect, useMemo, useState } from "react";
import { toast } from "sonner";
import { useLocation } from "wouter";

const DEFAULT_BRIEF = "Find founders and operators who need a provider to build custom AI workflows, automate operations, or produce practical AI video.";

export default function Home() {
  const utils = trpc.useUtils();
  const { user } = useAuth();
  const [, setLocation] = useLocation();
  const [brief, setBrief] = useState(DEFAULT_BRIEF);
  const [searchStep, setSearchStep] = useState<1 | 2 | null>(null);
  const overview = trpc.monitoring.overview.useQuery(undefined, { refetchInterval: 30_000 });
  const invalidateOverview = () => utils.monitoring.overview.invalidate();
  const agent = trpc.monitoring.agentStart.useMutation({
    onSuccess: async result => { await invalidateOverview(); result.syncError ? toast.error(result.sourceLabel) : toast.success(result.sync?.inserted ? `${result.sync.inserted} live posts checked.` : "Live X source checked — no new posts found."); },
    onError: error => toast.error(error.message),
  });
  const sync = trpc.monitoring.sync.useMutation({
    onSuccess: async result => { await invalidateOverview(); toast.success(`${result.inserted} new posts checked.`); },
    onError: async error => { await invalidateOverview(); toast.error(error.message); },
  });

  useEffect(() => { if (!agent.isPending) { setSearchStep(null); return; } setSearchStep(1); const timer = window.setTimeout(() => setSearchStep(2), 650); return () => window.clearTimeout(timer); }, [agent.isPending]);
  const activeBrief = overview.data?.monitors.find(({ monitor }) => monitor.status === "active") ?? overview.data?.monitors[0];
  const qualified = useMemo(() => getQualifiedPosts(overview.data?.posts ?? [], activeBrief?.monitor.id), [overview.data?.posts, activeBrief?.monitor.id]);
  const preview = useMemo(() => getDiscoverPreview(qualified, 6), [qualified]);
  const sourceNeedsAttention = activeBrief?.sync?.status && activeBrief.sync.status !== "healthy";
  const greeting = personalizedGreeting(user?.name);
  const runAgent = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); agent.mutate({ brief }); };

  return <DashboardLayout><div className="mx-auto max-w-6xl pb-8">
    <header className="flex items-center justify-between border-b border-[#eadfd2] pb-5"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-[#f1d7b9] text-[#8f4e38]"><Compass className="h-4 w-4" /></span><div><p className="text-[11px] font-bold text-[#a25d47]">{greeting}</p><h1 className="mt-0.5 text-xl font-extrabold tracking-[-0.055em]">Discover requests</h1></div></div><button onClick={() => activeBrief && sync.mutate({ monitorId: activeBrief.monitor.id })} disabled={!activeBrief || sync.isPending || agent.isPending} className="grid h-9 w-9 place-items-center rounded-xl border border-[#e8dacc] bg-white text-[#765d4a] transition hover:bg-[#fff8ef] disabled:opacity-40" title="Check live source"><RefreshCw className={`h-4 w-4 ${sync.isPending ? "animate-spin" : ""}`} /></button></header>
    <AgentBrief brief={brief} setBrief={setBrief} onSubmit={runAgent} pending={agent.isPending} searchStep={searchStep} agentResult={agent.data} sourceMessage={sourceNeedsAttention ? activeBrief?.sync?.lastError || "Source needs attention before a fresh check." : "One brief. One focused live search."} sourceNeedsAttention={Boolean(sourceNeedsAttention)} />
    <DiscoverPreview overview={overview} preview={preview} total={qualified.length} onReview={() => setLocation("/review")} onOpen={postId => setLocation(`/review?post=${postId}`)} />
  </div></DashboardLayout>;
}

function AgentBrief({ brief, setBrief, onSubmit, pending, searchStep, agentResult, sourceMessage, sourceNeedsAttention }: { brief: string; setBrief: (value: string) => void; onSubmit: (event: FormEvent<HTMLFormElement>) => void; pending: boolean; searchStep: 1 | 2 | null; agentResult: any; sourceMessage: string; sourceNeedsAttention: boolean }) { return <section className="mt-6 overflow-hidden rounded-[28px] border border-[#ead9c4] bg-[#fbf2e5] p-5 text-[#38291f] shadow-[0_16px_35px_rgba(105,68,38,0.06)] sm:p-7"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="flex items-center gap-2 text-[10px] font-bold uppercase tracking-[0.16em] text-[#a25d47]"><span className="h-1.5 w-1.5 rounded-full bg-[#be694d]" />Faro Agent</div><h2 className="mt-2 max-w-xl text-2xl font-extrabold tracking-[-0.055em] sm:text-3xl">Describe the client you want to find.</h2></div><span className="grid h-11 w-11 place-items-center rounded-2xl bg-[#f4dfc4] text-[#a5533c]"><Radar className="h-5 w-5" /></span></div><form onSubmit={onSubmit} className="mt-6"><Textarea value={brief} onChange={event => setBrief(event.target.value)} className="min-h-24 resize-none border-[#e3cdb1] bg-white text-sm leading-6 text-[#3b2d23] placeholder:text-[#b79a80] focus-visible:ring-[#bd674c]" placeholder="Who should Faro find?" /><div className="mt-3 flex flex-wrap items-center justify-between gap-3"><span className={`text-[10px] font-semibold ${sourceNeedsAttention ? "text-[#a55136]" : "text-[#9b765f]"}`}>{sourceMessage}</span><Button type="submit" disabled={pending || brief.trim().length < 12} className="h-12 rounded-2xl bg-[#b85f45] px-5 text-xs font-extrabold text-white shadow-[0_8px_18px_rgba(157,76,53,0.22)] transition hover:-translate-y-0.5 hover:bg-[#9f4d36] disabled:transform-none">{pending ? <><Loader2 className="mr-2 h-4 w-4 animate-spin" />Searching X</> : <><Sparkles className="mr-2 h-4 w-4" />Run Faro <ArrowRight className="ml-2 h-4 w-4" /></>}</Button></div></form>{searchStep ? <SearchProgress step={searchStep} /> : null}{agentResult && !pending ? <AgentOutcome result={agentResult} /> : null}</section>; }

function DiscoverPreview({ overview, preview, total, onReview, onOpen }: { overview: any; preview: any[]; total: number; onReview: () => void; onOpen: (postId: number) => void }) { return <section className="mt-7"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-extrabold tracking-[-0.03em]">Latest qualified requests</p><p className="mt-1 text-[10px] text-[#9a8a7b]">A quick scan of the strongest stored matches for this brief.</p></div><Button onClick={onReview} variant="outline" className="h-9 rounded-xl border-[#e3cdb8] bg-white px-3 text-xs font-bold text-[#814e38] hover:bg-[#fff8ef]">Open review <Inbox className="ml-1.5 h-3.5 w-3.5" /></Button></div>{overview.isError ? <div className="mt-4 flex min-h-44 flex-col items-center justify-center rounded-2xl border border-[#efd5c7] bg-[#fff8f3] px-5 text-center"><CircleAlert className="h-5 w-5 text-[#b45d43]" /><p className="mt-3 text-sm font-extrabold text-[#70412e]">Faro could not load stored requests.</p><p className="mt-1 max-w-md text-[10px] leading-5 text-[#9b725e]">Your saved review data was not changed. Retry the workspace before running another live search.</p><Button onClick={() => overview.refetch()} variant="outline" className="mt-4 h-9 rounded-xl border-[#e2c3b0] bg-white text-xs font-bold text-[#884d37] hover:bg-[#fff2e8]"><RefreshCw className="mr-1.5 h-3.5 w-3.5" />Retry loading</Button></div> : overview.isLoading ? <div className="mt-4 grid min-h-40 place-items-center rounded-2xl border border-[#eadfd2] bg-white"><Loader2 className="h-5 w-5 animate-spin text-[#b6a697]" /></div> : preview.length ? <div className="mt-4 overflow-hidden rounded-[22px] border border-[#eadfd2] bg-white"><div className="hidden grid-cols-[minmax(0,1fr)_76px_28px] gap-4 border-b border-[#f0e5da] bg-[#fffaf5] px-5 py-3 text-[9px] font-extrabold uppercase tracking-[0.13em] text-[#a28a78] sm:grid"><span>Request</span><span>Signal</span><span /></div>{preview.map(item => <DiscoverRow key={item.post.id} item={item} onOpen={() => onOpen(item.post.id)} />)}<button onClick={onReview} className="flex w-full items-center justify-between border-t border-[#f0e5da] bg-[#fffaf5] px-5 py-3 text-left text-[11px] font-bold text-[#85533e] hover:bg-[#fff5eb]"><span>Review all {total} qualified requests</span><ArrowRight className="h-3.5 w-3.5" /></button></div> : <div className="mt-4 flex min-h-40 flex-col items-center justify-center rounded-2xl border border-dashed border-[#eadfd2] bg-[#fffdfa] px-5 text-center"><Search className="h-5 w-5 text-[#b7a799]" /><p className="mt-3 text-sm font-bold">No qualified requests yet.</p><p className="mt-1 max-w-md text-[10px] text-[#9d8e80]">Run Faro to check the live source. Topic chatter stays out.</p></div>}</section>; }

function DiscoverRow({ item, onOpen }: { item: any; onOpen: () => void }) { const { post, monitorName } = item; const author = post.authorName || post.authorHandle || "Unknown source"; return <button onClick={onOpen} className="grid w-full gap-2 border-b border-[#f1e7de] px-4 py-4 text-left transition hover:bg-[#fffaf5] sm:grid-cols-[minmax(0,1fr)_76px_28px] sm:items-center sm:gap-4 sm:px-5"><div className="min-w-0"><div className="flex items-center gap-2"><p className="truncate text-xs font-extrabold text-[#3d2e23]">{author}</p><span className="hidden truncate text-[10px] text-[#a18b7a] sm:inline">{monitorName}</span></div><p className="mt-1.5 max-h-10 overflow-hidden text-[11px] leading-5 text-[#725e50]">{post.body}</p></div><span className="w-fit rounded-full bg-[#e7f3e9] px-2.5 py-1 text-[10px] font-extrabold text-[#3f7757]">{post.ruleScore}</span><ExternalLink className="hidden h-3.5 w-3.5 text-[#a06b55] sm:block" /></button>; }
function SearchProgress({ step }: { step: 1 | 2 }) { const scanning = step === 2; return <div className="mt-5 rounded-2xl border border-[#ead9c4] bg-white/70 p-3.5"><div className="flex items-center justify-between text-[10px] font-bold text-[#8b604a]"><span>{scanning ? "Scanning live X and qualifying requests" : "Preparing your search brief"}</span><span className="flex items-center gap-1.5 text-[#b15d44]"><span className="h-1.5 w-1.5 animate-pulse rounded-full bg-current" />{scanning ? "Live" : "Starting"}</span></div><div className="mt-2.5 h-2 overflow-hidden rounded-full bg-[#f2dfc8]"><div className={`h-full rounded-full bg-[#b85f45] transition-all duration-700 ease-out ${scanning ? "w-full animate-pulse" : "w-[42%]"}`} /></div></div>; }
function AgentOutcome({ result }: { result: { syncError: string | null; sourceStatus: string; sourceLabel?: string; sync: { inserted: number } | null } }) { const failed = Boolean(result.syncError); const message = failed ? result.sourceStatus === "payment_required" ? "Live X source needs account credit before Faro can fetch posts." : `${result.sourceLabel}. Faro saved the brief, but could not complete a live source check.` : result.sync?.inserted ? `${result.sync.inserted} live posts were checked. Review the strongest matches below.` : "Live X source checked. No new public posts matched this brief yet."; return <div className={`mt-5 flex items-center gap-2 rounded-2xl px-3 py-2.5 text-[11px] font-semibold ${failed ? "bg-[#fff0e9] text-[#a55136]" : "bg-[#eaf4e9] text-[#2f7147]"}`}>{failed ? <CircleAlert className="h-4 w-4 shrink-0" /> : <CheckCircle2 className="h-4 w-4 shrink-0" />}{message}</div>; }
