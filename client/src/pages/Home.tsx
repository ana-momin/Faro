import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { trpc } from "@/lib/trpc";
import { ArrowUpRight, Bot, Check, ChevronDown, CircleAlert, Crosshair, Filter, Loader2, Plus, Radio, RefreshCw, ShieldCheck, Sparkles, ThumbsDown, ThumbsUp, X } from "lucide-react";
import { FormEvent, useEffect, useMemo, useState } from "react";
import { toast } from "sonner";

type FeedStatus = "all" | "pending" | "approved" | "rejected";

function compactNumber(value: number) {
  return new Intl.NumberFormat("en", { notation: "compact", maximumFractionDigits: 1 }).format(value || 0);
}

function statusStyle(status: string | null | undefined) {
  if (status === "healthy") return "bg-[#e0f1e7] text-[#275c40]";
  if (status === "payment_required" || status === "rate_limited") return "bg-[#ffe9df] text-[#8a3e23]";
  if (status === "error" || status === "degraded") return "bg-[#f8dddd] text-[#8c3030]";
  return "bg-[#e7edf2] text-[#4b6376]";
}

function scoreTone(score: number) {
  if (score >= 70) return "bg-[#dff2e7] text-[#216542]";
  if (score >= 50) return "bg-[#e3f1fa] text-[#2e607c]";
  return "bg-[#edf1f3] text-[#5d7382]";
}

export default function Home() {
  const utils = trpc.useUtils();
  const [filter, setFilter] = useState<FeedStatus>("all");
  const [minimumScore, setMinimumScore] = useState(50);
  const [activeMonitorId, setActiveMonitorId] = useState<number | null>(null);
  const [hasInitializedFocus, setHasInitializedFocus] = useState(false);
  const [showCreator, setShowCreator] = useState(false);
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [goal, setGoal] = useState("People asking for help building custom AI workflows for a small business");
  const [name, setName] = useState("AI workflow opportunities");
  const [includeTerms, setIncludeTerms] = useState("AI workflow, automation, custom workflow");
  const [excludeTerms, setExcludeTerms] = useState("job, giveaway");
  const [xQuery, setXQuery] = useState('(\"AI workflow\" OR automation OR \"custom workflow\") -(job OR giveaway) -is:retweet');
  const overviewInput = useMemo(() => activeMonitorId ? { monitorId: activeMonitorId } : undefined, [activeMonitorId]);
  const overview = trpc.monitoring.overview.useQuery(overviewInput, { refetchInterval: 30_000 });

  const invalidate = () => utils.monitoring.overview.invalidate();
  const suggest = trpc.monitoring.suggest.useMutation({
    onSuccess: result => { setIncludeTerms(result.includeTerms.join(", ")); setExcludeTerms(result.excludeTerms.join(", ")); setXQuery(result.xQuery); toast.success(result.fallback ? "A focused deterministic query is ready." : `Personalized criteria ready via ${result.model}.`); },
    onError: () => toast.error("Couldn’t create a suggestion. You can still edit the query directly."),
  });
  const create = trpc.monitoring.create.useMutation({ onSuccess: result => { toast.success("Signal monitor saved."); setActiveMonitorId(result.monitorId); setShowCreator(false); invalidate(); }, onError: error => toast.error(error.message) });
  const seedDemo = trpc.monitoring.seedDemo.useMutation({ onSuccess: result => { toast.success(result.created ? "Clearly labeled sample signals added." : "Sample signals are already present."); invalidate(); }, onError: () => toast.error("Couldn’t add sample signals.") });
  const sync = trpc.monitoring.sync.useMutation({ onSuccess: result => { toast.success(`${result.inserted} public posts analyzed against your signal profile.`); invalidate(); }, onError: error => { toast.error("Sync status updated — check the source card."); console.warn(error.message); invalidate(); } });
  const review = trpc.monitoring.review.useMutation({ onSuccess: () => { toast.success("Human review decision saved."); invalidate(); }, onError: error => toast.error(error.message) });

  const allPosts = overview.data?.posts ?? [];
  const monitors = overview.data?.monitors ?? [];
  useEffect(() => {
    if (!hasInitializedFocus && monitors.length) {
      setActiveMonitorId(monitors[0].monitor.id);
      setHasInitializedFocus(true);
    }
  }, [hasInitializedFocus, monitors]);
  const activeMonitor = monitors.find(({ monitor }) => monitor.id === activeMonitorId)?.monitor ?? null;
  const activeSync = monitors.find(({ monitor }) => monitor.id === activeMonitorId)?.sync;
  const latestSync = activeSync ?? monitors.find(({ sync: monitorSync }) => monitorSync)?.sync;
  const sourceName = latestSync?.source === "filtered_stream" ? "X Stream" : latestSync?.source === "twitterapi_io" ? "Alt. X API" : "X Search";
  const posts = useMemo(() => allPosts.filter(({ post }) => (filter === "all" || post.reviewStatus === filter) && post.ruleScore >= minimumScore), [allPosts, filter, minimumScore]);
  const selected = posts.find(({ post }) => post.id === selectedId) ?? posts[0] ?? null;
  const strongMatches = allPosts.filter(({ post }) => post.ruleScore >= 70).length;
  const activeRequests = allPosts.filter(({ post }) => post.aiIntent.label === "Active help-seeking").length;

  function createMonitor(event: FormEvent<HTMLFormElement>) {
    event.preventDefault();
    create.mutate({ name, goal, xQuery, includeTerms: includeTerms.split(",").map(term => term.trim()).filter(Boolean), excludeTerms: excludeTerms.split(",").map(term => term.trim()).filter(Boolean), categories: ["personalized signals", "human review"] });
  }

  return <DashboardLayout><div className="relative mx-auto max-w-[1550px] overflow-hidden rounded-[30px] bg-[#f8fafb] px-4 py-5 md:px-8 md:py-8">
    <div className="pointer-events-none absolute -right-14 -top-20 h-72 w-72 rounded-full bg-[#d8eafb]/75 blur-[1px]" />
    <div className="pointer-events-none absolute right-64 top-36 h-36 w-36 rotate-12 rounded-[38px] bg-[#f7dbe1]/70" />
    <div className="pointer-events-none absolute -bottom-24 left-[20%] h-56 w-56 rounded-full border-[22px] border-[#dcecf9]/80" />
    <header className="relative z-10 flex flex-col justify-between gap-6 border-b border-[#dfe7ed] pb-7 lg:flex-row lg:items-end"><div><div className="mb-3 flex items-center gap-2 text-[10px] font-semibold uppercase tracking-[0.2em] text-[#6c7e8c]"><span className="h-2 w-2 rounded-full bg-[#8fc7ee]" /> Alpha X signal intelligence</div><h1 className="max-w-2xl text-3xl font-extrabold leading-[0.98] tracking-[-0.055em] text-[#141b22] sm:text-5xl sm:tracking-[-0.075em]">See the right signals.<br /><span className="text-[#5b7183]">Keep the decision human.</span></h1><p className="mt-4 max-w-xl text-sm font-light leading-6 text-[#5e7080]">Alpha X learns from the monitoring goal you define, bringing forward the public X posts with the clearest need, strongest topic fit, and most usable context—without sending outreach.</p></div><Button className="rounded-full bg-[#17232d] px-5 text-white hover:bg-[#263b4b]" onClick={() => setShowCreator(value => !value)}><Plus className="mr-2 h-4 w-4" /> New signal</Button></header>

    <section className="relative z-10 mt-5 flex flex-col gap-4 rounded-[20px] border border-[#cbdde8] bg-[#edf7fc]/80 px-5 py-4 md:flex-row md:items-center md:justify-between"><div><p className="text-[10px] font-semibold uppercase tracking-[0.15em] text-[#4d7893]">Personalized review loop</p><p className="mt-1 text-sm text-[#385b70]"><strong>1.</strong> Define what a good finding means. <strong>2.</strong> Review the signal profile. <strong>3.</strong> Sync public X posts. <strong>4.</strong> Decide manually.</p></div><Button variant="outline" className="shrink-0 rounded-full border-[#a7c7da] bg-white text-[#294b61]" onClick={() => setShowCreator(true)}><Sparkles className="mr-2 h-4 w-4" /> Tune a signal</Button></section>

    {showCreator && <section className="relative z-10 mt-6 rounded-[24px] border border-[#bed3e2] bg-white/85 p-5 shadow-[0_16px_50px_rgba(54,85,110,0.08)] backdrop-blur md:p-6"><div className="mb-5 flex items-start justify-between gap-6"><div><p className="text-xs font-medium uppercase tracking-[0.15em] text-[#6f8595]">Signal builder</p><h2 className="mt-1 text-2xl font-bold tracking-[-0.05em]">Describe your ideal finding.</h2><p className="mt-2 text-sm font-light text-[#647683]">Share the need, buyer context, and outcome you care about. Alpha X turns it into editable public-search criteria and a relevance profile.</p></div><button className="rounded-full p-2 hover:bg-[#edf3f7]" onClick={() => setShowCreator(false)} aria-label="Close signal builder"><X className="h-4 w-4" /></button></div><form onSubmit={createMonitor} className="grid gap-4 lg:grid-cols-2"><label className="grid gap-2 text-xs font-semibold uppercase tracking-[0.12em] text-[#667b8b]">Signal name<Input value={name} onChange={event => setName(event.target.value)} className="h-11 rounded-xl border-[#d7e3ea] bg-white text-sm normal-case tracking-normal" /></label><div className="flex items-end gap-2"><label className="grid flex-1 gap-2 text-xs font-semibold uppercase tracking-[0.12em] text-[#667b8b]">Desired finding<Textarea value={goal} onChange={event => setGoal(event.target.value)} className="min-h-20 rounded-xl border-[#d7e3ea] bg-white text-sm normal-case tracking-normal" /></label><Button type="button" variant="outline" className="h-10 rounded-xl border-[#c9d9e4]" onClick={() => suggest.mutate({ goal })} disabled={suggest.isPending}><Sparkles className="mr-2 h-4 w-4" /> {suggest.isPending ? "Mapping" : "Suggest"}</Button></div><label className="grid gap-2 text-xs font-semibold uppercase tracking-[0.12em] text-[#667b8b]">Topic concepts<Input value={includeTerms} onChange={event => setIncludeTerms(event.target.value)} className="h-11 rounded-xl border-[#d7e3ea] bg-white text-sm normal-case tracking-normal" /></label><label className="grid gap-2 text-xs font-semibold uppercase tracking-[0.12em] text-[#667b8b]">Exclude noise<Input value={excludeTerms} onChange={event => setExcludeTerms(event.target.value)} className="h-11 rounded-xl border-[#d7e3ea] bg-white text-sm normal-case tracking-normal" /></label><label className="grid gap-2 text-xs font-semibold uppercase tracking-[0.12em] text-[#667b8b] lg:col-span-2">Validated X rule<Input value={xQuery} onChange={event => setXQuery(event.target.value)} className="mono h-11 rounded-xl border-[#d7e3ea] bg-white text-xs normal-case tracking-normal" /></label><div className="flex flex-wrap items-center justify-between gap-3 lg:col-span-2"><p className="flex items-center gap-2 text-xs font-light text-[#627587]"><ShieldCheck className="h-4 w-4 text-[#448169]" /> Signals guide manual review only—never outreach.</p><Button type="submit" disabled={create.isPending} className="rounded-full bg-[#17232d] px-5 text-white"><Radio className="mr-2 h-4 w-4" /> Save signal</Button></div></form></section>}

    <section className="relative z-10 mt-6 grid gap-4 xl:grid-cols-[1.2fr_0.8fr_0.8fr_0.8fr]"><Metric label="Active signals" value={String(monitors.length).padStart(2, "0")} detail={activeMonitor ? "Focused signal selected" : "Choose one to personalize"} /><Metric label="Strong matches" value={String(strongMatches).padStart(2, "0")} detail="70+ relevance profile" accent="blue" /><Metric label="Active requests" value={String(activeRequests).padStart(2, "0")} detail="Explicit need detected" accent="pink" /><Metric label="Live source" value={sourceName} detail={latestSync?.latencyLabel ?? "Create or select a signal"} /></section>

    <section className="relative z-10 mt-6 grid gap-6 xl:grid-cols-[minmax(0,1.6fr)_390px]"><div className="rounded-[25px] border border-[#d9e5ec] bg-white/80 p-4 shadow-[0_16px_50px_rgba(54,85,110,0.07)] backdrop-blur sm:p-5"><div className="flex flex-col gap-4 border-b border-[#e2eaef] pb-4"><div className="flex flex-col justify-between gap-4 lg:flex-row lg:items-start"><div><p className="text-[11px] font-semibold uppercase tracking-[0.16em] text-[#6c8394]">Personalized findings</p><h2 className="mt-1 text-xl font-bold tracking-[-0.05em]">{activeMonitor ? activeMonitor.name : "Your highest-fit X posts"}</h2><p className="mt-1 max-w-xl text-[11px] text-[#738898]">{activeMonitor ? activeMonitor.goal : "Select a signal to focus the feed. Scores weight topic fit, need, decision context, specificity, freshness, and intent support."}</p></div>{monitors.length > 0 && <label className="flex min-w-[220px] items-center gap-2 text-xs font-semibold text-[#617484]"><Crosshair className="h-4 w-4 text-[#477895]" /><select className="w-full rounded-full border border-[#d4e0e7] bg-white px-3 py-2 font-medium" value={activeMonitorId ?? ""} onChange={event => { const id = event.target.value ? Number(event.target.value) : null; setActiveMonitorId(id); setSelectedId(null); }}><option value="">All signals</option>{monitors.map(({ monitor }) => <option key={monitor.id} value={monitor.id}>{monitor.name}</option>)}</select></label>}</div><div className="flex flex-wrap items-center justify-between gap-3"><div className="flex rounded-full bg-[#eef3f6] p-1">{(["all", "pending", "approved", "rejected"] as FeedStatus[]).map(value => <button key={value} onClick={() => setFilter(value)} className={`rounded-full px-3 py-1.5 text-xs transition ${filter === value ? "bg-white font-semibold text-[#223340] shadow-sm" : "text-[#6b7e8d]"}`}>{value[0].toUpperCase() + value.slice(1)}</button>)}</div><label className="flex items-center gap-2 text-xs text-[#617484]"><Filter className="h-3.5 w-3.5" /> Relevance <select className="rounded-full border border-[#d4e0e7] bg-white px-2 py-1.5" value={minimumScore} onChange={event => setMinimumScore(Number(event.target.value))}><option value={0}>All</option><option value={50}>50+ relevant</option><option value={70}>70+ strong fit</option></select></label></div></div><div className="mt-3 grid gap-2">{overview.isLoading && <div className="flex min-h-56 items-center justify-center text-sm text-[#6b8090]"><Loader2 className="mr-2 h-4 w-4 animate-spin" /> Calibrating your review queue</div>}{!overview.isLoading && !posts.length && <div className="soft-grid flex min-h-64 flex-col items-center justify-center rounded-2xl px-6 text-center"><div className="mb-4 flex items-end gap-1.5"><span className="h-3 w-1.5 rounded-full bg-[#a6cfe8]" /><span className="h-6 w-1.5 rounded-full bg-[#7eb9dd]" /><span className="h-10 w-1.5 rounded-full bg-[#eeb6c7]" /><span className="h-6 w-1.5 rounded-full bg-[#7eb9dd]" /><span className="h-3 w-1.5 rounded-full bg-[#a6cfe8]" /></div><h3 className="font-bold tracking-[-0.04em]">No findings meet this relevance threshold yet.</h3><p className="mt-2 max-w-sm text-sm font-light text-[#647887]">Broaden the signal, lower the threshold, or sync again. Alpha X will keep clearly labeled demo rows separate from live findings.</p><div className="mt-5 flex gap-2"><Button variant="outline" className="rounded-full border-[#c8dbe8] bg-white" onClick={() => setShowCreator(true)}><Plus className="mr-2 h-4 w-4" /> Tune signal</Button><Button className="rounded-full bg-[#17232d]" onClick={() => seedDemo.mutate()} disabled={seedDemo.isPending}>Load sample</Button></div></div>}{posts.map(({ post, monitorName }) => <button key={post.id} onClick={() => setSelectedId(post.id)} className={`group grid gap-3 rounded-2xl border px-4 py-4 text-left transition hover:-translate-y-0.5 hover:shadow-md md:grid-cols-[72px_1fr_auto] ${selected?.post.id === post.id ? "border-[#99c0db] bg-[#f4f9fc]" : "border-transparent hover:border-[#d5e3eb] hover:bg-[#fbfdfe]"}`}><div className={`flex h-12 w-12 items-center justify-center rounded-2xl text-sm font-extrabold ${scoreTone(post.ruleScore)}`}><span>{post.ruleScore}</span></div><div className="min-w-0"><div className="mb-2 flex flex-wrap items-center gap-2"><span className="text-xs font-bold tracking-[-0.02em]">{post.authorHandle ? `@${post.authorHandle}` : post.authorName || "Unknown author"}</span><span className="text-[11px] text-[#718594]">{new Date(post.postedAt).toLocaleString()}</span><span className="text-[10px] font-medium uppercase tracking-[0.1em] text-[#6f8595]">{monitorName}</span></div><p className="line-clamp-2 text-sm leading-6 text-[#263640]">{post.body}</p><div className="mt-2 flex flex-wrap gap-2"><Badge className="rounded-full bg-[#e3f0fa] text-[10px] font-medium text-[#315a75] hover:bg-[#e3f0fa]">{post.aiIntent.label}</Badge>{post.ruleScore >= 70 && <Badge className="rounded-full bg-[#e1f1e6] text-[10px] font-medium text-[#306e4a] hover:bg-[#e1f1e6]">Strong fit</Badge>}{post.source === "demo" && <Badge className="rounded-full bg-[#f6e4e8] text-[10px] font-medium text-[#874d5c] hover:bg-[#f6e4e8]">Demo sample</Badge>}</div></div><div className="flex items-start gap-1 self-start"><span className={`rounded-full px-2 py-1 text-[10px] font-semibold uppercase tracking-[0.08em] ${post.reviewStatus === "approved" ? "bg-[#e1f1e6] text-[#306e4a]" : post.reviewStatus === "rejected" ? "bg-[#f8e3e3] text-[#8c4141]" : "bg-[#edf1f3] text-[#667986]"}`}>{post.reviewStatus}</span><ChevronDown className="mt-1 h-3.5 w-3.5 text-[#7c8f9b] transition group-hover:translate-y-0.5" /></div></button>)}</div></div><aside className="rounded-[25px] border border-[#d9e5ec] bg-[#fcfeff]/90 p-5 shadow-[0_16px_50px_rgba(54,85,110,0.07)] backdrop-blur">{selected ? <PostDetail item={selected} monitorGoal={monitors.find(({ monitor }) => monitor.name === selected.monitorName)?.monitor.goal ?? ""} onReview={decision => review.mutate({ postId: selected.post.id, decision })} reviewPending={review.isPending} /> : <div className="flex min-h-64 flex-col items-center justify-center text-center"><CircleAlert className="h-6 w-6 text-[#7199b5]" /><p className="mt-3 text-sm text-[#607685]">Select a finding to see why it fits.</p></div>}</aside></section>

    <section className="relative z-10 mt-6 grid gap-4 lg:grid-cols-2">{monitors.map(({ monitor, sync: monitorSync }) => <article key={monitor.id} className={`rounded-[20px] border bg-white/75 p-4 transition ${monitor.id === activeMonitorId ? "border-[#9bc5dd] shadow-[0_10px_28px_rgba(54,85,110,0.1)]" : "border-[#d7e4eb]"}`}><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-semibold uppercase tracking-[0.14em] text-[#768b9b]">Signal profile</p><h3 className="mt-1 font-bold tracking-[-0.04em]">{monitor.name}</h3><p className="mt-1 line-clamp-2 text-xs font-light text-[#647988]">{monitor.goal}</p></div><span className={`rounded-full px-2 py-1 text-[10px] font-semibold uppercase tracking-[0.08em] ${statusStyle(monitorSync?.status)}`}>{monitorSync?.status?.replaceAll("_", " ") ?? "ready"}</span></div><div className="mt-4 flex flex-wrap items-center justify-between gap-2 border-t border-[#e5edf1] pt-3"><button className="max-w-[55%] truncate text-left text-[10px] font-medium text-[#527187] hover:underline" onClick={() => { setActiveMonitorId(monitor.id); setSelectedId(null); }}>{monitor.id === activeMonitorId ? "Focused in findings" : "Focus these findings"}</button><Button size="sm" variant="outline" className="h-8 rounded-full border-[#c9dce8] text-xs" onClick={() => sync.mutate({ monitorId: monitor.id })} disabled={sync.isPending}><RefreshCw className={`mr-1.5 h-3.5 w-3.5 ${sync.isPending ? "animate-spin" : ""}`} /> Sync</Button></div><p className="mt-2 text-[11px] text-[#708694]">{monitorSync?.latencyLabel ?? "Not synced yet"}{monitorSync?.lastSyncedAt ? ` · ${new Date(monitorSync.lastSyncedAt).toLocaleString()}` : ""}</p></article>)}</section>
  </div></DashboardLayout>;
}

function Metric({ label, value, detail, accent }: { label: string; value: string; detail: string; accent?: "blue" | "pink" }) {
  return <article className={`rounded-[21px] border border-[#d8e4ea] bg-white/75 p-4 shadow-[0_8px_25px_rgba(54,85,110,0.045)] ${accent === "blue" ? "border-l-4 border-l-[#a8d6f1]" : accent === "pink" ? "border-l-4 border-l-[#efbaca]" : ""}`}><p className="text-[10px] font-semibold uppercase tracking-[0.14em] text-[#758a9a]">{label}</p><p className="mt-3 text-2xl font-extrabold tracking-[-0.06em] text-[#18232c]">{value}</p><p className="mt-1 text-xs font-light text-[#6a7f8e]">{detail}</p></article>;
}

function PostDetail({ item, monitorGoal, onReview, reviewPending }: { item: { post: any; monitorName: string }; monitorGoal: string; onReview: (decision: "approved" | "rejected") => void; reviewPending: boolean }) {
  const { post, monitorName } = item;
  const engagement = Object.values(post.engagement as Record<string, number>).reduce((sum: number, value: unknown) => sum + Number(value || 0), 0);
  return <div><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-semibold uppercase tracking-[0.14em] text-[#73899a]">Personalized fit</p><h2 className="mt-1 text-xl font-extrabold tracking-[-0.055em]">Why this deserves review</h2></div><div className={`flex h-12 w-12 items-center justify-center rounded-2xl text-base font-extrabold ${scoreTone(post.ruleScore)}`}>{post.ruleScore}</div></div>{monitorGoal && <div className="mt-4 rounded-2xl border border-[#d6e5ee] bg-[#f8fbfd] p-3"><p className="text-[10px] font-semibold uppercase tracking-[0.14em] text-[#6e8697]">Your desired outcome</p><p className="mt-1 text-xs leading-5 text-[#425d70]">{monitorGoal}</p></div>}<div className="mt-4 rounded-2xl bg-[#f1f6f9] p-4"><p className="text-xs font-bold">{post.authorHandle ? `@${post.authorHandle}` : post.authorName || "Unknown author"}</p><p className="mt-2 text-sm leading-6 text-[#273944]">{post.body}</p><div className="mt-3 flex items-center justify-between text-[11px] text-[#6b8090]"><span>{new Date(post.postedAt).toLocaleString()}</span><span>{compactNumber(engagement)} engagement</span></div></div><div className="mt-5"><p className="text-[10px] font-semibold uppercase tracking-[0.14em] text-[#73899a]">Match breakdown</p><div className="mt-2 grid gap-2">{(post.scoreExplanation as Array<{ label: string; points: number }>).map(component => <div className="flex items-center justify-between rounded-xl border border-[#e1eaef] px-3 py-2 text-xs" key={component.label}><span className="text-[#516a7b]">{component.label}</span><span className={`font-bold ${component.points > 0 ? "text-[#34795a]" : "text-[#a24848]"}`}>{component.points > 0 ? "+" : ""}{component.points}</span></div>)}</div></div><div className="mt-5 rounded-2xl border border-[#d6e5ee] bg-[#edf7fd] p-4"><div className="flex items-center gap-2"><Bot className="h-4 w-4 text-[#477895]" /><p className="text-[10px] font-semibold uppercase tracking-[0.14em] text-[#477895]">Intent interpretation</p></div><p className="mt-2 text-sm font-bold text-[#25465d]">{post.aiIntent.label} · {Math.round(post.aiIntent.confidence * 100)}%</p><p className="mt-1 text-xs leading-5 text-[#527187]">{post.aiIntent.rationale}</p><p className="mt-2 text-[10px] text-[#6b8798]">{post.aiIntent.fallback ? "Deterministic relevance profile" : post.aiIntent.model} · advisory only</p></div><div className="mt-5 flex flex-wrap gap-2">{post.postUrl ? <a href={post.postUrl} target="_blank" rel="noreferrer" className="inline-flex h-9 items-center rounded-full border border-[#c9dbe6] bg-white px-3 text-xs font-semibold text-[#36576c] hover:bg-[#f2f8fb]">Open on X <ArrowUpRight className="ml-1.5 h-3.5 w-3.5" /></a> : <span className="inline-flex h-9 items-center rounded-full bg-[#f5e6e9] px-3 text-xs font-medium text-[#865061]">Demo sample — no X link</span>}{post.reviewStatus === "pending" ? <><Button className="h-9 rounded-full bg-[#276e4c] text-xs hover:bg-[#1f5a3e]" disabled={reviewPending} onClick={() => onReview("approved")}><ThumbsUp className="mr-1.5 h-3.5 w-3.5" /> Approve</Button><Button variant="outline" className="h-9 rounded-full border-[#e7c2c2] text-xs text-[#9c3e3e] hover:bg-[#fff5f5]" disabled={reviewPending} onClick={() => onReview("rejected")}><ThumbsDown className="mr-1.5 h-3.5 w-3.5" /> Reject</Button></> : <span className="inline-flex h-9 items-center rounded-full bg-[#eff4f6] px-3 text-xs font-semibold text-[#566d7d]"><Check className="mr-1.5 h-3.5 w-3.5" /> {post.reviewStatus}</span>}</div><p className="mt-5 text-[11px] leading-5 text-[#6b8190]">Decision state: {post.reviewStatus}. Alpha X stores a human-review label only; it never triggers messaging, posting, or outreach.</p><p className="mt-3 text-[10px] uppercase tracking-[0.12em] text-[#7990a0]">Matched {monitorName}</p></div>;
}
